# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
build/
.pub-cache/
.pub/
# pubspec.lock IS committed — this is an application, not a library, so builds
# should be reproducible.

# Generated code — regenerate with:
#   dart run build_runner build --delete-conflicting-outputs
*.g.dart
*.freezed.dart
*.drift.dart

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
.metadata

# Android
android/.gradle/
android/local.properties
android/key.properties
android/app/*.jks
android/app/*.keystore
*.apk
*.aab

# iOS / macOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
macos/Pods/
**/.DS_Store

# Windows / Linux desktop
windows/flutter/ephemeral/
linux/flutter/ephemeral/

# Node dependencies (the ETL toolchain under tools/etl)
node_modules/

# ETL working files — large downloads and intermediate data, never committed
tools/etl/.cache/
tools/etl/out/
# Built product packs + manifest. Reproducible via `npm run build:off`, and in
# production published to a GitHub Release, so never tracked.
tools/etl/dist/
*.zip
*.parquet
*.jsonl
*.jsonl.gz
*.csv.gz
duckdb.exe

# Reference database artifacts. The BLS pack is committed as a built asset
# (assets/data/bls.sqlite); the much larger OFF packs are published as GitHub
# Releases instead of being tracked here.
tools/etl/out/*.sqlite
off_*.sqlite
off_*.sqlite.zst

# Local env
.env
.env.local

# Flutter tool crash reports
flutter_*.log
